// Logging prefix
export const LOG_PREFIX = "[Nomad IP]";

// AWS API Gateway constants
export const STAGE_NAME = "nomadip";
export const GATEWAY_NAME_PREFIX = "nomad-ip-";

// Retry and rate limiting configuration
export const MAX_RETRIES = 3;
export const RETRY_DELAY_MS = 1000;
export const RATE_LIMIT_DELAY_MS = 500;

// State management limits
export const MAX_DOMAIN_INDEX_ENTRIES = 1000;
